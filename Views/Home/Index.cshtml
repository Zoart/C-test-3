@model Product[]
@{
    ViewData["Title"] = "Home Page";
}

@* <div data-proudctid="@Model.ProductID" data-stocklevel="@ViewBag.World" class="text-center"> *@

<table>
<thead>
<tr><th>Name</th><th>Price</th></tr>
</thead>
<tbody>
@foreach (Product p in Model) {
<tr>
<td>@p.Name</td>
<td>@($"{p.Price:C2}")</td>
</tr>
}
</tbody>
</table>

    <p>@ViewData["Hello"] @ViewBag.World</p>

    @* <p>Stock level:
        @switch ((int)ViewBag.StockLevel) {
            case 0:
                @: Out of Stock
                break;
            case 1:
            case 2:
            case 3:
                <b> Low Stock (@ViewBag.StockLevel) </b>
                break;
            default:
                @: @ViewBag.StockLevel in Stock
                break;
        }
    </p> *@

    <p>If statement</p>
    @if (ViewBag.StockLevel == 0)
    {
        @: Zero stock
    }
    else if (ViewBag.StockLevel == 2)
    {
        @: Stock level is two from if
    }
</div>
